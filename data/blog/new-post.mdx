---
title: 文章标题
date: '2026-08-17'
tags:
  - 标签1
  - 标签2
categories:
  - 分类
draft: true
summary: >-
  一句话摘要，用于博客卡片、RSS feed 和 SEO 描述。
authors:
  - default
---

## 小标题

在这里写正文，支持标准 Markdown、**加粗**、`行内代码`、[链接](https://example.com) 和列表：

- 项目一
- 项目二

代码块支持标题和高亮：

```ts title="example.ts"
function greet(name: string): string {
  return `你好，${name}！`
}
```

> [!NOTE]
> 提示块（`NOTE` / `TIP` / `WARNING` / `IMPORTANT` / `CAUTION`）均受支持。

![配图](/static/blog/new-post/screenshot.webp)

{/* 提示：图片放到 public/static/blog/new-post/ 目录，用绝对路径引用。 */}
{/* 写完后把 draft 改为 false 即可发布。 */}
